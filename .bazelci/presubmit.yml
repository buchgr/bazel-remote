---
validate_config: 1
tasks:
  ubuntu1804:
    platform: ubuntu1804
    shell_commands:
    - "set -e"
    - "echo 1"
    - ".bazelci/fail.sh"
    - "echo 3" # This should not be run, because the previous command failed.
    #- ".bazelci/fail.sh" # When this is uncommented, the build actually fails.
    build_targets:
    - "..."
